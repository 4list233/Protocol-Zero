# ============================================
# Protocol Zero - Environment Variables Template
# ============================================
# 
# INSTRUCTIONS:
# 1. Copy this file to .env (for local development)
# 2. Fill in your actual values (never commit .env to git)
# 3. For production, set these in your hosting platform (Vercel, etc.)
#
# NOTE: All .env* files are gitignored except .env.example
# ============================================

# ============================================
# GEMINI API (for scraper translation)
# ============================================
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ============================================
# NOTION DATABASE (Alternative to Knack)
# ============================================
# Only needed if NOT using Knack (USE_KNACK_DATABASE=false or unset)
# Get these values from: https://www.notion.so/my-integrations
NOTION_API_KEY=secret_your_integration_token_here
NOTION_DATABASE_ID_PRODUCTS=your_products_database_id_here
NOTION_DATABASE_ID_VARIANTS=your_variants_database_id_here

# Optional: Cache TTL in seconds (default: 120)
NOTION_CACHE_TTL_SECONDS=120

# ============================================
# FIREBASE (Authentication & Firestore)
# ============================================
# Required for Firebase Auth (client-side)
# Get these from: https://console.firebase.google.com/
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abcdefghijk
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# Required for Firebase Admin SDK (server-side auth verification)
# Option A: Single JSON string (recommended)
# FIREBASE_SERVICE_ACCOUNT_KEY='{"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}'

# Option B: Individual fields (alternative)
# FIREBASE_ADMIN_PROJECT_ID=your-project-id
# FIREBASE_ADMIN_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
# FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# ============================================
# KNACK DATABASE (Primary - Products, Orders, etc.)
# ============================================
# Required if using Knack as your database backend
# Get these from: https://builder.knack.com/
KNACK_APPLICATION_ID=your-knack-application-id
KNACK_REST_API_KEY=your-knack-rest-api-key

# Enable Knack database (set to 'true' to use Knack instead of Notion/Firestore)
USE_KNACK_DATABASE=true

# Optional: Knack Object Keys (defaults provided in knack-config.ts)
# KNACK_OBJECT_KEY_PRODUCTS=object_6
# KNACK_OBJECT_KEY_VARIANTS=object_7
# KNACK_OBJECT_KEY_ORDERS=object_10
# KNACK_OBJECT_KEY_CLIPS=object_11
# KNACK_OBJECT_KEY_SIGNUPS=object_12
# KNACK_OBJECT_KEY_USERS=object_8
# KNACK_OBJECT_KEY_ACCOUNTS=object_2

# Optional: Knack Field Keys (defaults provided in knack-config.ts)
# See shop/lib/knack-config.ts for all available field mappings

# ============================================
# DATABASE (Prisma/PostgreSQL)
# ============================================
# Required if using Prisma models (User, Post, Comment, PlayDate)
# DATABASE_URL=postgresql://user:password@host:5432/database

# ============================================
# APPLICATION SETTINGS
# ============================================
# Optional: Base URL for the application
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# ============================================
# NOTES:
# ============================================
# - All variables starting with NEXT_PUBLIC_ are exposed to the browser
# - Never commit actual secrets to git
# - .env is for local development (gitignored)
# - Set these in your hosting platform for production
# - All scripts and applications load from this root .env file
